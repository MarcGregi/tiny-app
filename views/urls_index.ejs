<!DOCTYPE html>
<html lang="en">
 <head>
      <title>TinyApp</title>
  </head>

<body>

  <header>
      <% include ./partials/_header %>
    </header>

  <ul>

   <% for (var shorterUrl in urls) { %>
    <li><%= shorterUrl + urls[shorterUrl] %></li>
    <form method="POST" action="/urls/<%= shorterUrl%>/delete">
    <button type="submit">Delete URL</button>
    </form>
        <%}%>
  </ul>


</form>
</body>
</html>